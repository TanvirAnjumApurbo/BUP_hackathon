fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0

# Not used at stage 1, but pinned into the image now so the LP optimizer and the
# LLM client land later without a slow full rebuild of the container.
numpy>=1.26,<3.0
scipy>=1.11,<2.0
httpx>=0.27,<1.0
